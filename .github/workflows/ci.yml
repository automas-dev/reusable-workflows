on:
  push:
    branches:
      - main

  pull_request:
    types:
      - opened
      - reopened
      - synchronize

env:
  FOO: foo
  BAR: bar

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Install Python
        uses: actions/setup-python@v6
        with:
          python-version: 3.11

      - name: Install YAML Lint
        run: pip install yamllint

      - name: Run YAML Lint
        run: yamllint -c yamllint.yml .

  increment_version:
    name: Increment Version
    uses: automas-dev/reusable-workflows/.github/workflows/increment_version.yml@main
    if: ${{ github.ref == 'refs/heads/main' }}
    secrets: inherit
    # with:
    #   create-aliases: true

    needs: [lint]

    permissions:
      contents: write

  test_stuff:
    uses: automas-dev/reusable-workflows/.github/workflows/test_pass_vars.yml@tf_vars
    secrets: inherit

    permissions:
      contents: write
