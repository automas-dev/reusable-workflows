on:
  workflow_call:

jobs:
  tmp:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - run: echo env got $BAR

      - run: echo env got $FOO
        env:
          FOO: ${{ env.FOO }}

      - run: echo env got $VAR_FOO
        env:
          FOO: ${{ vars.VAR_FOO }}
